services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /home/debian/ToneBot/:/app/Discord/
      - /media/archive/Discord/songs:/app/Discord/songs
      - /etc/localtime:/etc/localtime
    restart: always
